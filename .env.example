# Paper-trading credentials for IbGatewayContainerIT (Testcontainers).
#
# Copy this file to `.env` and fill in real values. `.env` is git-ignored.
# Real environment variables TWS_USERID / TWS_PASSWORD take precedence over this file.
TWS_USERID=your_paper_username
TWS_PASSWORD=your_paper_password
